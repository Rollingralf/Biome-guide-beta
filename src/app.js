const biomeList=document.getElementById('biomeList');const biomeContent=document.getElementById('biomeContent');const searchInput=document.getElementById('searchInput');function init(){renderBiomeList(BIOMES);searchInput.addEventListener('input',handleSearch);}function renderBiomeList(biomes){biomeList.innerHTML='';biomes.forEach(biome=>{const item=document.createElement('div');item.className='biome-item';item.innerHTML=`${biome.icon} ${biome.name}`;item.addEventListener('click',()=>selectBiome(biome,item));biomeList.appendChild(item);});}function selectBiome(biome,element){document.querySelectorAll('.biome-item').forEach(el=>el.classList.remove('active'));element.classList.add('active');renderBiomeContent(biome);}function renderBiomeContent(biome){const html=`<div class="biome-header"><div class="biome-icon">${biome.icon}</div><div class="biome-title"><h2>${biome.name}</h2><p>${biome.description}</p></div></div><div class="biome-section"><h3>📦 Items</h3><div class="items-grid">${biome.items.map(item=>`<div class="item-card"><div class="item-name">${item.name}</div><div class="item-description">${item.use}</div></div>`).join('')}</div></div>`;biomeContent.innerHTML=html;}function handleSearch(){const term=searchInput.value.toLowerCase();const filtered=BIOMES.filter(b=>b.name.toLowerCase().includes(term)||b.description.toLowerCase().includes(term));renderBiomeList(filtered);}document.addEventListener('DOMContentLoaded',init);